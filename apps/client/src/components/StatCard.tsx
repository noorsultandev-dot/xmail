import {motion} from 'framer-motion';import type {LucideIcon} from 'lucide-react';export function StatCard({label,value,icon:Icon,index=0}:{label:string;value:string|number;icon:LucideIcon;index?:number}){return <motion.div initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{delay:index*.05}} className="glass rounded-2xl p-5"><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-[.18em] text-slate-500">{label}</p><Icon size={18} className="text-brand-primary"/></div><div className="mt-3 font-display text-3xl font-bold">{value}</div></motion.div>}
