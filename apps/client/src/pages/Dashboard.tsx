import {useEffect,useMemo,useState} from 'react';import {BarChart3,MousePointerClick,Send,Eye} from 'lucide-react';import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip} from 'recharts';import {api} from '../lib/api';import {StatCard} from '../components/StatCard';
type Row={id:string;name:string;sent:number;opens:number;clicks:number;openRate:number};export default function Dashboard(){const [rows,setRows]=useState<Row[]>([]);useEffect(()=>{api<Row[]>('/api/analytics/global').then(setRows)},[]);const total=useMemo(()=>rows.reduce((a,r)=>({sent:a.sent+r.sent,opens:a.opens+r.opens,clicks:a.clicks+r.clicks}),{sent:0,opens:0,clicks:0}),[rows]);return <div className="space-y-6"><div><p className="text-sm font-bold uppercase tracking-[.25em] text-brand-primary">Global overview</p><h1 className="font-display text-4xl font-bold">All environments, one pulse.</h1></div><div className="grid gap-4 md:grid-cols-4"><StatCard label="Sent" value={total.sent} icon={Send}/><StatCard label="Opens" value={total.opens} icon={Eye} index={1}/><StatCard label="Clicks" value={total.clicks} icon={MousePointerClick} index={2}/><StatCard label="Environments" value={rows.length} icon={BarChart3} index={3}/></div><div className="glass rounded-2xl p-5 h-80"><h2 className="font-display text-xl font-semibold mb-4">Environment comparison</h2><ResponsiveContainer width="100%" height="85%"><BarChart data={rows}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="sent" fill="#4F46E5" radius={[8,8,0,0]}/><Bar dataKey="opens" fill="#14B8A6" radius={[8,8,0,0]}/><Bar dataKey="clicks" fill="#F59E0B" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div></div>}
