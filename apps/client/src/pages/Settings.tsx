import {useEffect,useState} from 'react';import {api} from '../lib/api';export default function Settings(){const [theme,setTheme]=useState(localStorage.theme??'system');const [animation,setAnimation]=useState('full');useEffect(()=>{const dark=theme==='dark'||(theme==='system'&&matchMedia('(prefers-color-scheme:dark)').matches);document.documentElement.classList.toggle('dark',dark);localStorage.theme=theme},[theme]);async function save(){await api('/api/settings',{method:'PUT',body:JSON.stringify({appearance:{theme,animation,density:'comfortable'}})})}return <div className="space-y-6"><div><p className="text-sm uppercase tracking-[.25em] text-brand-primary font-bold">Global settings</p><h1 className="font-display text-4xl font-bold">Make XMail yours.</h1></div><div className="glass rounded-2xl p-6 grid gap-5 md:grid-cols-2"><label className="font-semibold">Theme<select className="input mt-2" value={theme} onChange={e=>setTheme(e.target.value)}><option>system</option><option>light</option><option>dark</option></select></label><label className="font-semibold">Animation intensity<select className="input mt-2" value={animation} onChange={e=>setAnimation(e.target.value)}><option>full</option><option>reduced</option><option>off</option></select></label><button className="btn btn-primary md:col-span-2" onClick={save}>Save settings</button></div><div className="glass rounded-2xl p-6"><h2 className="font-display text-xl font-semibold">Domain deliverability</h2><p className="mt-2 text-slate-500">Configure SPF, DKIM and DMARC once for your sending domain. Every environment on that domain benefits from the same authentication foundation.</p></div></div>}
